<Application
    x:Class="SampleApp.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SampleApp"
    xmlns:viewModels="using:SampleApp.ViewModels"
    RequestedTheme="Light">
    <Application.Resources>
        <!--ADDED TO SAMPLE TO DEMONSTRATE SimpleAdo.Sqlite
            Added a resource for the MainViewModel class.
            Note that since my ViewModels are in a separate 'MyApplication.ViewModels' namespace -
            I needed to add the following namespace reference line above:
            xmlns:viewModels="using:MyApplication.ViewModels" -->
        <viewModels:MainViewModel x:Key="MainViewModel" />
    </Application.Resources>
</Application>
